## Destravar pedido `71e20016…` agora (Plano A)\n\n### O que será feito\n\n1. Marcar o pedido `71e20016-…` (200 créditos, status `processing`, travado há ~7 min sem novas execuções) como `refunded` com motivo `worker_stalled`.\n2. Estornar os 200 créditos restantes (nada foi farmado) para o saldo do cliente via `refund_order_remainder(order_id, 'work